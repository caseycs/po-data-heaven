name: all Order

description: bla bla bla

sql: >
 fds fdsa  fds fdsa
 fadsf
 sdafdas
 fdsa

parameters:
  sr_id:
    title: date
    input: date
    idOfEntity: order
    default: todayStart
  date:
    title: date
    input: date
    default: todayFinish
    default: now
    default: currentWeekStart
    default: currentWeekFinish

limit: 20

order: x ASC

orientation: vertical

columns:
  a:
    format: number
    idOfEntity: orde f dsff sadsr
