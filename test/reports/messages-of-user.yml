name: Messages of user

description: Messages of user report description

sql: >
 SELECT * FROM `messages` WHERE user_id = :user_id

parameters:
  user_id:
    name: User ID
    idOfEntity: user

columns:
  id:
    idOfEntities: user
