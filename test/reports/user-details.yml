name: User details

description: User details report description

sql: >
 SELECT * FROM `users` WHERE id = :id

parameters:
  id:
    name: test
    idOfEntity: user

columns:
  id:
    idOfEntities: user
